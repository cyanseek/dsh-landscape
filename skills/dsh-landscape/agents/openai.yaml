interface:
  display_name: "DSH Landscape"
  short_description: "Preflight a DSH capability change before acting"
  default_prompt: "Use $dsh-landscape to run a read-only preflight for my DeepSeek Harness change, show what already exists, what not to rebuild, and the safest next action."
